<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Contact import -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Force remove RECEIVE_SMS if added by dependencies -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" tools:node="remove" />

    <!-- Local notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for notification alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- WorkManager exact alarm scheduling -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Reschedule alarms after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".SITApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIT.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Disable WorkManager's default ContentProvider initializer so we can use HiltWorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Alarm receiver — handles both tickle alarms and post-reboot rescheduling.
             exported="false": the TICKLE_ALARM PendingIntent is package-restricted
             (setPackage, TickleScheduler.kt) but not component-explicit, so the
             action must stay in the intent-filter for the OS to resolve delivery;
             exported="false" is what actually blocks third-party apps from
             broadcasting TICKLE_ALARM at this receiver. BOOT_COMPLETED is a
             protected system broadcast and is still delivered when unexported. -->
        <receiver
            android:name=".service.TickleAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xaymaca.sit.TICKLE_ALARM" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
